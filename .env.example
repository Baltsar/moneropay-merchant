# MoneroPay Dashboard — environment template
# Copy this file to .env and replace placeholder values. Do not commit .env.
# See README.md and docs/TESTING.md for setup instructions.
#
# ===== NODE MODE =====
# "local" = run own monerod (250GB disk, 6-24h initial sync, full privacy)
# "remote" = connect to external node (5GB disk, ready in minutes, reduced privacy)
NODE_MODE=local

# Remote node settings (only used when NODE_MODE=remote)
# Also set DAEMON_HOST and DAEMON_PORT so wallet-rpc connects to the remote node.
# Use a known-good node (e.g. node.sethforprivacy.com if the default does not resolve).
REMOTE_NODE_HOST=node.moneroworld.com
REMOTE_NODE_PORT=18089
DAEMON_HOST=node.moneroworld.com
DAEMON_PORT=18089

# ===== MONEROD SETTINGS (local mode only) =====
PRUNE_BLOCKCHAIN=true

# ===== DATABASE =====
POSTGRES_USERNAME=moneropay
POSTGRES_PASSWORD=CHANGE_ME_RANDOM_STRING
POSTGRES_DATABASE=moneropay

# ===== MONEROPAY =====
MONEROPAY_BIND=0.0.0.0:5000
MONEROPAY_ZERO_CONF=false

# ===== DASHBOARD =====
DASHBOARD_PORT=3000
FIAT_CURRENCY=USD

# Frontend build-time variables (Vite). Used when building the dashboard image
# or when running `npm run build` locally. In Docker, NODE_MODE is passed as build arg.
# VITE_MONEROPAY_URL=/api
# VITE_NODE_RPC_URL=/node
# VITE_NODE_MODE=local
# VITE_FIAT_CURRENCY=USD
# VITE_USE_MOCK=false
